// Please do not edit this file as it was generated using a script

import React, { FC } from 'react';

import Delete from '@iconify-icons/carbon/delete';
import { Icon as ReactIcon } from '@iconify/react/offline';

import Icon, { IIconProps } from '../Icon';

const DeleteIcon: FC<Omit<IIconProps, 'icon'>> = ({
  size,
  className,
  color,
}) => {
  return (
    <Icon
      icon={<ReactIcon icon={Delete} />}
      size={size}
      className={className}
      color={color}
    />
  );
};

export default DeleteIcon;
