// Please do not edit this file as it was generated using a script

import React, { FC } from 'react';

import Settings from '@iconify-icons/carbon/settings';
import { Icon as ReactIcon } from '@iconify/react/offline';

import Icon, { IIconProps } from '../Icon';

const SettingsIcon: FC<Omit<IIconProps, 'icon'>> = ({
  size,
  className,
  color,
}) => {
  return (
    <Icon
      icon={<ReactIcon icon={Settings} />}
      size={size}
      className={className}
      color={color}
    />
  );
};

export default SettingsIcon;
