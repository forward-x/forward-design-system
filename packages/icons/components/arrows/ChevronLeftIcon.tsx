// Please do not edit this file as it was generated using a script

import React, { FC } from 'react';

import ChevronLeft from '@iconify-icons/carbon/chevron-left';
import { Icon as ReactIcon } from '@iconify/react/offline';

import Icon, { IIconProps } from '../Icon';

const ChevronLeftIcon: FC<Omit<IIconProps, 'icon'>> = ({
  size,
  className,
  color,
}) => {
  return (
    <Icon
      icon={<ReactIcon icon={ChevronLeft} />}
      size={size}
      className={className}
      color={color}
    />
  );
};

export default ChevronLeftIcon;
