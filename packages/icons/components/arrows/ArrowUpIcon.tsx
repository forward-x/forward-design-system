// Please do not edit this file as it was generated using a script

import React, { FC } from 'react';

import ArrowUp from '@iconify-icons/carbon/arrow-up';
import { Icon as ReactIcon } from '@iconify/react/offline';

import Icon, { IIconProps } from '../Icon';

const ArrowUpIcon: FC<Omit<IIconProps, 'icon'>> = ({
  size,
  className,
  color,
}) => {
  return (
    <Icon
      icon={<ReactIcon icon={ArrowUp} />}
      size={size}
      className={className}
      color={color}
    />
  );
};

export default ArrowUpIcon;
