// Please do not edit this file as it was generated using a script

import React, { FC } from 'react';

import Wallet from '@iconify-icons/carbon/wallet';
import { Icon as ReactIcon } from '@iconify/react/offline';

import Icon, { IIconProps } from '../Icon';

const WalletIcon: FC<Omit<IIconProps, 'icon'>> = ({
  size,
  className,
  color,
}) => {
  return (
    <Icon
      icon={<ReactIcon icon={Wallet} />}
      size={size}
      className={className}
      color={color}
    />
  );
};

export default WalletIcon;
