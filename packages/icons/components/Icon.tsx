import React, { FC } from 'react';
import styles from './index.module.scss';

export interface IIconProps {
  className?: string;
  size: 'S' | 'M' | 'L';
}

const Icon: FC<IIconProps> = ({ size, className }) => {
  return <>h</>;
};

export default Icon;
