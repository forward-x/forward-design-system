// Please do not edit this file as it was generated using a script

import React, { FC } from 'react';

import View from '@iconify-icons/carbon/view';
import { Icon as ReactIcon } from '@iconify/react/offline';

import Icon, { IIconProps } from '../Icon';

const ViewIcon: FC<Omit<IIconProps, 'icon'>> = ({ size, className, color }) => {
  return (
    <Icon
      icon={<ReactIcon icon={View} />}
      size={size}
      className={className}
      color={color}
    />
  );
};

export default ViewIcon;
