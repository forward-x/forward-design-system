{
  "packages": ["packages/*", "site"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "ignoreChanges": ["**/*.md", ".github/**", "**/*.stories.*", "site/**"],
  "command": {
    "version": {
      "message": "chore(release): publish"
    }
  }
}
