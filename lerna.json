{
  "packages": ["packages/*", "site"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "ignoreChanges": ["**/*.md", ".github/**", "**/*.stories.*"],
  "command": {
    "version": {
      "message": "chore(release): publish"
    }
  }
}
